# `<tap-searchbar>`

Mobile search bar component. Features animated cancel button on focus and smooth interaction with `<tap-page>`.

## Example

<gbp-example name="tap-searchbar" src="https://esm.sh/@mantou/tap-ui/elements/searchbar">

```json
[
  { "placeholder": "Search apps or content..." },
  { "placeholder": "Pre-filled query", "value": "TapUI" },
  { "placeholder": "Disabled search bar", "disabled": true }
]
```

</gbp-example>

## API

<gbp-api name="tap-searchbar" src="/src/elements/searchbar.ts"></gbp-api>

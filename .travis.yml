language: node_js

node_js: 12

branches:
  only:
    - master
    - /^v?\d+(\.\d+){1,2}.*/

deploy:
  script: npm run build:examples
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
    tags: true

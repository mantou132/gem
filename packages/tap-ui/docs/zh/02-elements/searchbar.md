# `<tap-searchbar>`

移动端搜索栏组件。聚焦时带有取消按钮动画，支持与 `<tap-page>` 滚动配合。

## Example

<gbp-example name="tap-searchbar" src="https://esm.sh/@mantou/tap-ui/elements/searchbar">

```json
[
  { "placeholder": "搜索应用或内容..." },
  { "placeholder": "已输入关键词", "value": "TapUI" },
  { "placeholder": "禁用搜索栏", "disabled": true }
]
```

</gbp-example>

## API

<gbp-api name="tap-searchbar" src="/src/elements/searchbar.ts"></gbp-api>

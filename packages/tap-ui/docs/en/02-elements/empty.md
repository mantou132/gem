# `<tap-empty>`

Empty state component. Provides placeholder cues and messages when page or list content is empty.

## Example

<gbp-example name="tap-empty" src="https://esm.sh/@mantou/tap-ui/elements/empty">

```json
[
  {},
  { "text": "No search results" },
  { "text": "Your cart is empty" }
]
```

</gbp-example>

## API

<gbp-api name="tap-empty" src="/src/elements/empty.ts"></gbp-api>
